# Progreso de fases — rent-collector-dashboard

> Estados: `pendiente | en curso | completada`. Una fase por sesión.
> Las notas de una fase completada resumen QUÉ se construyó, archivos tocados, resultado de
> `lint`/`build`/`test:e2e` y referencias a `DECISIONS.md`. Es el contexto que lee la siguiente sesión.
>
> El backlog D1–D10 (`docs/MEJORAS.md`) precede a este harness. Su estado real vive en ese doc
> (D1–D3, D5, D6, D9, D10 ✅ aplicados; D4/D7/D8 se retoman como Fases 2–4 abajo).

| Fase | Descripción | Estado | Fecha | Notas |
|---|---|---|---|---|
| 1 | Vigencia de contrato y ajuste de renta en el formulario de inquilino | completada | 2026-07-22 | Formulario de alta/edición captura `contractStartDate/EndDate`, `nextMonthlyAmount`, `adjustmentDate`; tarjeta de inquilino muestra vigencia + chip "Vigente/Fuera de vigencia" y el ajuste de renta. Subcomponente `ContractFields` reutilizado en ambos diálogos. Solo `src/app/(dashboard)/propiedades/[id]/page.tsx`. `lint` ✅ · `build` + TS ✅. Backend confirmó los 4 campos en los GET (canal, 2026-07-22). Nota: `nextMonthlyAmount` llega como string → coerción con `Number()`. |
| 2 | Auth / login (D4, G1/G2) | completada | 2026-07-22 | `api.ts`: token en módulo + `Authorization: Bearer` en `request()` + redirect a /login en 401 (excepto /auth/login) + `login()`/`getMe()`. Store: `token`/`authReady`/`hydrateAuth`/`login`/`logout`; `landlordId` ya NO viene de env (se deriva de /me), init 0. Página `/login`, `AuthGate` en el layout del dashboard, botón "Salir" en el header. Verificado end-to-end contra :3001: login 200, /me 200, protegido con token 200 / sin token 401, password malo 401. Suite e2e adaptada a la sesión: mock de `/me`+`/auth/login`, siembra de token en localStorage, `hydrateAuth` no expulsa por error de red (solo 401), y nuevo `e2e/auth.spec.ts` (redirect a login, login ok, login inválido). `lint` ✅ · `build`+TS ✅ · `test:e2e` 26/26 ✅. Ver [[decision-token-storage]] en DECISIONS.md. |
| 3 | Tipos generados desde OpenAPI (D7, G7) | completada | 2026-07-22 | `openapi-typescript` instalado; `npm run openapi:types` genera `src/lib/backend-schema.ts` desde `:3001/docs-json`. Guardia de drift a nivel de tipo en `src/lib/backend-schema.check.ts` (rompe el build si el backend deja de proveer un campo que el UI exige). Refactor completo de `types.ts` a consumir el schema queda pendiente (mayor). Drift detectado: OpenAPI declara decimales como `number` pero runtime serializa `string` (reportado al backend). `lint`/`build`/TS ✅. |
| 4a | Auth por cookie httpOnly + BFF (fundamento seguro de D8) | completada | 2026-07-23 | BFF `src/app/api/[...path]/route.ts` reemplaza el rewrite: lee cookie httpOnly `rc_token` y reenvía a `BACKEND_URL` con Bearer. Login guarda cookie y devuelve solo `{landlord}`; store pasó de `token`+localStorage a `authenticated` derivado de `/me`; `logout` limpia cookie. e2e adaptados (sesión con estado, sin localStorage) + test de facturas hecho determinista. `lint`/`build`/TS ✅ · `test:e2e` 26/26 ✅ · verificado end-to-end contra el backend real (cookie httpOnly, `/me` 200/401, token ausente del body). Cierra la deuda XSS de [[decision-token-storage]]. |
| 4b | Reportes → Server Component con prefetch en servidor (D8) | completada | 2026-07-23 | `src/lib/server-api.ts` (fetch en servidor con la cookie de 4a); `reportes/page.tsx` es Server Component que prefetchea el reporte y lo pasa a `ReportesView` (cliente, interacción de mes); fallback a fetch cliente si no hay sesión de servidor (mantiene e2e verdes). Con 4c, el reporte ya se ve en el **primer paint SSR** (sin spinner). `lint`/`build`/TS ✅ · `test:e2e` 26/26 ✅. |
| 4c | Guard de auth en servidor (middleware) + "no flash" | completada | 2026-07-23 | `src/middleware.ts` redirige a /login si falta la cookie `rc_token` (guard server-side); `AuthGate` dejó de bloquear (solo sincroniza `landlordId` desde /me y redirige de respaldo en 401); `DataBootstrap` dispara con `authReady`. e2e adaptados: cookie sembrada en Playwright, login/logout con Set-Cookie, `BACKEND_URL` a puerto muerto para SSR hermético. Verificado end-to-end: `/reportes` con cookie trae el reporte en el HTML SSR (0 skeleton); `/dashboard` sin cookie → 307 /login, con cookie → 200. `lint`/`build`/TS ✅ · `test:e2e` 26/26 ✅. |
| 4d | Extender SSR al resto de páginas (Dashboard, Pagos, Propiedades, Facturas) | pendiente (opcional) | | Replicar el patrón de 4b (función en `src/lib/server-api.ts` + `page.tsx` Server Component que prefetchea y pasa datos a una vista cliente) página por página. Sin incógnitas de arquitectura (4a/4c ya sientan la base: sesión legible en servidor + guard en middleware). El AuthGate ya no bloquea, así que estas páginas ya renderizan sin spinner, pero sus **datos** aún se cargan en cliente. Cada página que use datos por-landlord debe cuidar el fallback cliente para no romper los e2e (mock del navegador). Trabajo incremental, no bloqueante. |
