Vas a trabajar en la Fase $ARGUMENTS de rent-collector-dashboard.

1. Lee `PROGRESS.md` y confirma que las fases anteriores están completas. Si no, detente y repórtalo.
2. Lee la Fase $ARGUMENTS en `PLAN.md` §8 y las referencias que cite (`docs/CONTRATOS_API.md`, `docs/MEJORAS.md`, `AGENTS.md`).
3. Si la fase está marcada como **bloqueada** (depende de trabajo del backend, p. ej. §2.9 "backend listo" o OpenAPI Q9), detente y avísame: no se codifica contra un contrato que el backend aún no expone.
4. Presenta un plan de implementación breve (lista de tareas en orden) y espera confirmación antes de escribir código. Para fases grandes, usa modo plan.
5. Implementa tarea por tarea, con un commit por tarea (español imperativo, sin firma de Claude). Respeta los guardrails de `PLAN.md` §10.
